<div
  class="
    d-flex
    justify-content-between
    flex-wrap flex-md-nowrap
    align-items-center
    pt-3
    pb-2
    mb-3
    border-bottom
  "
>
  <h1 class="h2">DIMMs Tested</h1>
  <div class="btn-toolbar mb-2 mb-md-0">
    <div class="btn-group me-2">
      <button
        onclick="exportTableToExcel()"
        type="button"
        class="btn btn-sm btn-outline-secondary"
      >
        Export
      </button>
    </div>
  </div>
</div>
<div class="table-responsive mb-5 pb-5">
  <form action="/api/v1.0/getDimmByDate" method="post">
    <caption>
      From:
    </caption>
    <input type="text" name="start" value="" style="width: 170px" />
    <caption>
      To:
    </caption>
    <input type="text" name="end" value="" style="width: 170px" />
    <button class="btn btn-dark btn-sm">Apply</button>
  </form>

  <table
    id="table"
    class="table table-striped table-sm table-hover align-middle display"
  >
    <thead>
      <tr>
        <th>Date Tested</th>
        <th>Serial Number</th>
        <th>Tester</th>
        <th>Result</th>
        <th>Comments</th>
      </tr>
    </thead>
    <tbody>
      <% dimms.forEach(function(dimm){ %>
      <tr>
        <td style="width: 10%">
          <%= moment(dimm.testedDate).format("DD/MM/YYYY") %>
        </td>
        <td style="width: 10%"><%= dimm.assetId %></td>
        <td style="width: 10%"><%= dimm.tester %></td>
        <td><%= dimm.result %></td>
        <td><%= dimm.comments %></td>
      </tr>
      <% }) %>
    </tbody>
  </table>
</div>
